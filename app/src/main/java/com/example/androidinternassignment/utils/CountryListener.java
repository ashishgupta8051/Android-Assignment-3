package com.example.androidinternassignment.utils;

import com.example.androidinternassignment.model.CountryDetails;

public interface CountryListener {

    void onClick(CountryDetails countryDetails);
}
