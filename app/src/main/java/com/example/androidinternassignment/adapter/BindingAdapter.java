package com.example.androidinternassignment.adapter;

import android.content.Context;
import android.net.Uri;
import android.view.View;
import android.widget.ImageView;
import android.widget.ProgressBar;

import com.github.twocoffeesoneteam.glidetovectoryou.GlideToVectorYou;
import com.github.twocoffeesoneteam.glidetovectoryou.GlideToVectorYouListener;

public class BindingAdapter {

/*    @androidx.databinding.BindingAdapter("android:imageUrl")
    public static void setImageURL(ImageView imageView, String url){
        try{
            imageView.setAlpha(0f);
            GlideToVectorYou.init().with(imageView.getContext()).withListener(new GlideToVectorYouListener() {
                @Override
                public void onLoadFailed() {
                    //progressBar.setVisibility(View.GONE);
                }

                @Override
                public void onResourceReady() {
                    //progressBar.setVisibility(View.GONE);
                }
            }).load(Uri.parse(url), imageView);

        }catch (Exception e){

        }
    }*/
}
